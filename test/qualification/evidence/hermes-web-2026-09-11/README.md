# Hermes 真实 Web 验收证据（2026-09-11）

当前具备真实聊天、联网搜索、经批准的工作目录文件读写、脚本生成和恢复能力；不能据此声称整个产品目标已完成。对话搜索 404、工具耗时数分钟、内置账号迁移未执行是明确的剩余问题。

入口：https://himawari.siyi.win 。沿用原 Cloudflare 登录。欢迎对话“开始使用 Himawari”；置顶“Hermes 功能验收”保留九轮成功、取消和失败的实际历史。

- `real-service-summary.json`：限定本任务 Owner/Agent 与两个 Thread 的只读数据库核查、工具交付意图、资源清理、文件摘要、预算和重启证据。
- `controlled-tests.json`：192 文件/1984 项完整测试、23 文件/565 项 tooling，均通过且零跳过；保留中间失败原因。属于受控测试。
- `browser-checks.json`：实际 HTTPS 浏览器流程；手机使用视口和触摸模拟，没有实体手机测试。
- `theme-matrix.json`：实际两主题六色组合的中性背景、文字和状态颜色。
- `browser-budget.json`：实际 Web 构建大小，原预算没有放宽。
- `desktop.png`、`mobile.png`、`model-menu.png`、`process.png`：直接从正式服务截图，未经图像编辑；只展示验收内容。悬浮翻译图标来自浏览器扩展。

## 真实与受控边界

成功读取、搜索、写脚本的工具在 Hermes Worker 中执行，结果持久化后经 Agent 现有披露校验及交付意图返回真实 GLM 模型。文件存在/内容由 SSH 独立回读；拒绝的文件不存在，没有创建执行 Job。Python 脚本只做 AST 解析，未运行或写入 crontab。Exa 是真实公开搜索摘录，未打开网页全文。本目录不保存第三方网页全文、原始 Trace、配置、凭据或浏览器令牌。

同一安装树的六项 Linux 资格使用固定合成输入与真实主机/网络边界，不能替代上述 Web 流程。最后重启后确实完成了新一轮模型续聊，而非只检查 HTTP 状态或 systemd active。

## 与原型比较

| 项目 | 正式实现与核查 |
| --- | --- |
| 布局与字体 | 保留侧栏、居中聊天、底部输入、按需详情；系统字体栈和深色中性色与基准一致。长记录真实滚动。 |
| 品牌 | 使用已确认 Logo 的原造型和实际浅色底块；没有把原 PNG 宣称为透明底，未覆盖原资源。 |
| 主题 | Light/Dark 与六色独立，切换主题色不改变正文、中性背景或成功/错误/等待色。 |
| 输入与菜单 | 真正可用的文本附件、发送/停止、键盘换行；模型与思考深度来自 Pi 能力。仅显示实际“执行”。 |
| 正文 | 在独立 React 组件渲染安全 Markdown；链接需用户点击，禁用原 HTML 和远程图片。 |
| 逐轮过程 | 展示实际工具名称、输入、输出、状态、耗时；没有可披露 thinking 摘要时明确说明。工具 JSON 置于折叠过程而非主正文。 |
| 审批 | 打开正式审批详情并确认/拒绝，未照搬原型的模拟批准按钮；等待时间独立。 |
| 手机 | 390px 视口无页面水平溢出，侧栏与详情可收起，触摸目标至少 44px；实体软键盘未验。 |
| 真实能力限制 | 管理模块保留原入口和未启用标识；搜索列表的生产缺口仍如实记录。 |

## 已知失败与未验范围

- 对话列表搜索 POST `/api/thread-search/v1/prepare` 返回 404；不等于已成功的互联网 `web_search`。
- GLM 曾只声称等待批准，实际未发起工具。该轮不算工具或审批成功；后续真实调用才完成拒绝验收。
- DeepSeek 真实返回 429；GLM minimal/low 有成功证据。刷新后下一轮选择恢复配置默认值。
- 工具本身约 3 分 12 秒，整轮工作约 4–5 分钟，审批等待另计；长历史加载也慢，性能不算通过。
- 仍用 Cloudflare 产品登录；没有擅自撤销已有 Owner 会话切换内置账号。
- 预算中未知调用仍保留预留。总确认费用与未知上界为 0.301426 美元，低于 1 美元；不是实际账单金额。

完整过程和剩余实施范围见 `docs/execution/plans/2026-09-10-control-center-local-acceptance-plan.md`。源代码基于 `099eefc1a5cec454cc4a5e68e984f08714c53014` 的任务改动验证；中间结果里的基准 HEAD 不能代替最终产物字节摘要。
