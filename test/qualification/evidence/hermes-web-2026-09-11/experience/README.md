# 聊天运行体验修复验收（2026-09-11）

当前：体验修复及同次重复核验优化已部署 Hermes，真实搜索完成，但整体速度仍未达到目标。最新时间线见 `performance-comparison.json`，完整检查与部署证据见 `verification.json`。下文早期未发布状态为历史记录。

## 早期候选验证记录

源码实现、完整本地检查及 Hermes 独立候选资格已完成；尚未切换正式服务。此次停机、备份和替换安装目录命令被自动审批拒绝，读取确认正式服务 PID 仍为 489813，原安装目录未移动。用户批准的联网搜索设置尚未在正式账号开启，不能将候选验证表述为线上修复完成。

`verification.json` 区分本地完整测试、受控浏览器和 Linux 候选实际探针。`controlled-mobile.png` 是正式网页构建连接受控服务的截图，其中思考与工具事件为合成验收输入；它不证明模型或工具实际执行。

完整 `npm test` 通过 1989 项：构建阶段 311238 ms，测试阶段 440709 ms，没有 300 秒截断；完整命令结果为 `local_passed`。本轮首次完整测试发现合同样例、功能列表、ICU 样例和 PRD 摘要未同步，修正后完整重跑通过。`npm run check` 及文档严格校验通过。

Hermes 独立候选六组真实探针均通过，包括 Pi 工具、服务组合、允许网络、拒绝出口、Worker 丢失清理和 Exa 搜索。第一次网络探针被其固定临时根检查拒绝，未发生网络执行；核验并改用本项目既有 0700 验收目录后通过。探针不包含真实模型聊天。依赖安装最终使用项目的完整 `npm ci --ignore-scripts` 和审核过的 SQLite 原生构建流程；未采用会改变精确锁文件的补装结果。

下一步须先获得正式切换的明确授权，再按已核验 Hermes Runbook 创建恢复点、保存旧安装、保持原正式路径切换、重新跑最终路径探针并签署启动。随后实际验证 DeepSeek、开启搜索、天气耗时、聊天内文件批准/拒绝以及刷新、停止和重连。未发布、推送或创建 PR。

完整字节校验在 Hermes 已安装候选上连续三次实测 3173、3138、3056 ms，摘要均为 `27f42787975d15ed3e3c2dbecc1bea996cef921d548b1f0f682a31ab75fb34f6`。每次重新读取全部字节，无跨调用结果缓存。这只是完整性校验的耗时，不是天气聊天端到端耗时。


## 当前补充结果

上文候选未发布的说明已被后续实际发布取代，最新分项状态见 `verification.json`。正式体验版本的最终路径六项探针、签名启动、DeepSeek 实际工具调用、固定 Exa 授权、聊天内批准写文件、草稿与下一轮模型刷新保留均已执行。`live-weather-progress.png` 和 `live-inline-approval.png` 为正式 HTTPS 页面截图。搜索三次仍约一分钟，性能未通过；随后性能改动仍在验证，不能把受控清理证据分段提速当作整轮提速。


## 2026-09-11 同次重复核验优化的正式结果

冻结源码后的完整 `npm test` 构建与 1999 项测试通过，`npm run check` 与文档严格校验通过。前两次完整命令在仍有源码编辑时发生 `ARTIFACT_BUILD_INPUT_MISMATCH`，未进入测试；不是测试通过，也不是 300 秒截断。

性能版本已在 Hermes 原前缀部署，六项最终路径探针通过、恢复点 `before-performance-2026-09-11-v2` 校验成功，既有能力声明语义核对不变。准备配置复制遗漏导致启动准备首次失败；补齐私密副本并重新执行签名、配置与能力核验后启动，未跳过检查。Agent 于 02:05:21.384Z、Worker 于 02:05:20.375Z 就绪（UTC），fence 31。发布目录字节摘要为 `2b9a034f551d47a8e13b39b95a4132aee3dd4e4a612ae3beaba8d91a9577a86a`。安装身份改变后，沿用用户已批准的固定 Exa 范围重新开启搜索设置。

相同 GLM minimal、相同查询词、limit 3、单次真实 Web 搜索：优化前工具 61326 ms、整轮 82753 ms；优化后工具 53043 ms、整轮 133126 ms。前后模型开始至工具意图分别为 7226/33533 ms，工具结果至最终正文分别为 8459/40553 ms；本次模型阶段更慢，不能声称整轮提速。两个 Run 均 completed，环境 released、cleanup confirmed，SQLite quick_check=ok。单次前后观察不足以估计延迟分布或将全部差额归因于代码。具体白名单时间线与截图见 `test/qualification/evidence/hermes-web-2026-09-11/experience/`。

当前性能目标未达到。每次核验仍读取全部安装文件；尚未实现不可变发布、内核按需完整性验证或跨调用缓存。将发布写权限与运行账号分离、保护版本目录和可信摘要、按版本升级重启，是正在向用户说明的架构方案，未经确认不得写成已接受决策或实施生产权限调整。原 Spec/Plan 继续 active。
